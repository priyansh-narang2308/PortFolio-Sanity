# Example environment variables for Twin Portfolio
# Copy this file to `.env.local` and replace placeholders with real values.

# Sanity (client-side values are NEXT_PUBLIC)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=develop
NEXT_PUBLIC_SANITY_API_VERSION=2025-11-09

# Clerk (auth)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_your_publishable_key
CLERK_SECRET_KEY=sk_live_your_secret_key

# OpenAI / ChatKit
NEXT_PUBLIC_CHATKIT_WORKFLOW_ID=wf_your_workflow_id
OPENAI_API_KEY=sk-your-openai-key

# Sanity server tokens (server-only — do NOT expose these in the browser)
SANITY_SERVER_API_TOKEN=skr_your_server_token
SANITY_API_TOKEN=sk_your_api_token

# Notes:
# - Never commit your real secret keys. Keep them in `.env.local` (which is ignored).
# - Prefixes with NEXT_PUBLIC_ are exposed to client code — avoid using that prefix for secret keys.